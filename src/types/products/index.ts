export type ProductType = {
    id: number;
    title: string;
    price: number;
}

export type BrandType = {
    id: number;
    name: string;
}

export type SizeType = {
    id: number;
    name: string;
}